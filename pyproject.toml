[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "aseer-cultural-fidelity"
version = "0.1.0"
description = "Source-anchored benchmark utilities for cultural transformation fidelity"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]
